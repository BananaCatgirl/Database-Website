extends ../layout.pug 

block content 
    h1 Delete Book

    if bookinstances.length > 0

        p you need to delete these book instances first before you can delete the book
        ul 
            each instance in bookinstances 
                li
                    a(href= instance.url) title: #{book.title} - id: #{instance.id}
          
    else 
        p do you really want to delete this book?

        form(method="POST" action="")
            .form-group 
                input#bookid.form-control(type="hidden" name="bookid" required="true" value=book.id)

            button.btn.btn-danger(type="submit") Delete
        
