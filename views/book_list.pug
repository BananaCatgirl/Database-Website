extends layout

block content
  h1= title

  ul
    each book in book_list
      li
        div( style="background-color: grey; min-height:50px; margin:10px;" )
          a(href=book.url) #{book.title} 
          |  (#{book.author.name})

    else
      li There are no books.
